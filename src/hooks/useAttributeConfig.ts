import { getAttributeElement } from "../lib/getAttributeElement";

const useAttributeConfig = () => {
  const attributes = getAttributeElement();
  return attributes;
};

export default useAttributeConfig;
